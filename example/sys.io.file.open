#!bin/jse

// opens a read-only file descriptor to self, then prints content to stdout using bash+cat.

var x = sys.io.file.open(sys.main, "r"); // r, w, or rw

sys.exec('bash', '-c', 'cat <&'+x);

sys.io.file.close(x);

